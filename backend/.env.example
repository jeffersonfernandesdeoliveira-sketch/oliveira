# Exemplo de variáveis de ambiente para o backend
# Defina a chave da API do Gemini/Generative Language aqui
# NÃO coloque a chave real neste arquivo — crie um arquivo local `backend/.env` com sua chave.
GEMINI_API_KEY=your_gemini_api_key_here

# Porta do servidor
PORT=3000
